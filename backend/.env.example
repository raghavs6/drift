# Copy to .env and fill in (never commit .env)
# Trip planner uses Anthropic — required for POST /api/plan-trip

ANTHROPIC_API_KEY=

# Optional — for future RIDB / Supabase server integrations
RIDB_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
