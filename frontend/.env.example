# Optional — override API (defaults to http://localhost:8000 in dev)
# VITE_API_BASE=http://localhost:8000

# Supabase browser client — use anon public key from Dashboard → Settings → API
# (Do not use the service role key in the frontend.)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
